<p><%= attachment_image_tag(@item, :image, :fill, 70, 70, fallback: "no-image-icon.jpg") %></p>
<h1><%= @item.name %></h1>
<p><%= @item.introduction %></p>
<p><%= @item.price %></p>

<!--考え中-->
<!--%= form_with model:cart_item,local:true do |f| %>-->
<!--    %= f.label :amount, "数量" %>-->
<!--    %= f.text_field :amount, @current_item_array %>-->
<!--% end %>-->

<%= link_to 'カートに入れる', cart_item_path(cart_item)%>
