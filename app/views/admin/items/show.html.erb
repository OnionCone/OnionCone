<div class="container">
  <div class="row">
    <div class="col-2">
      <h3>商品詳細</h3>
      <%= attachment_image_tag @item, :image, format: 'jpeg', size: "150x150" %>
      <p>商品名　<%= @item.name %></p>
      <p>商品説明　<%= @item.introduction %></p>
      <p>ジャンル<%= @item.genre.name %></p>
      <p>税込価格</br>（税抜価格）　<%= @item.price_tax.floor %>(<%= @item.price%>)円</p>
      <!--<p>販売ステータス<= @item.is_deleted %></p>-->
      <p>販売ステータス
        <% if  @item.is_deleted %>
          <%= '販売停止中' %>
        <% else %>
          <%= '販売中' %>
        <% end %>
      </p>
      <%= link_to "編集する", edit_admin_item_path(@item), class: 'btn btn-sm btn-success' %>
        </div>
      </div>
    </div>